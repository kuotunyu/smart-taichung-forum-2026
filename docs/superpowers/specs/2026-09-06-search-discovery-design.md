# Search discovery and readable forum notes

The user approved the preceding SEO recommendations and asked for direct execution, with handoff only where their access is necessary.

## Design

Keep the canonical GitHub Pages URL and the current visual design. Make all five chapters visible on initial load with or without JavaScript. Open the primary session notes and source groups by default; readers can still collapse details. Keep supplementary technical modules collapsible. Existing anchors, bilingual text, search, theme controls and print support must continue working.

Use a concise title and visible introduction identifying the 2026 forum, notes, summary and unofficial status. Preserve the distinction between speaker claims and compiler interpretation. Do not invent source timestamps, verification claims or new factual material. Keep actual publication date and update modification dates consistently.

Keep one canonical indexable page in this iteration. Splitting out session pages is deferred until there is a distinct editorial purpose, to avoid duplicating the same notes. Remove ineffective keyword metadata and sitemap priority/changefreq. Correct documentation: llms.txt is optional convenience, IndexNow requires an actual notification with keyLocation for this project path and does not submit to Google.

## Operations and scope

Inspect Search Console if the authenticated property is accessible. Submit the sitemap and request indexing after deployment if access permits. Otherwise give an exact owner handoff. Notify IndexNow after verifying the deployed content. Prepare a useful share draft but do not message other people or publish social posts without a chosen destination and explicit authorization.

## Acceptance

- Initial rendered page exposes all five chapters, primary notes and source links.
- Existing deep links, session filtering, bilingual control, theme and mobile layout remain usable.
- JSON-LD parses, canonical and sitemap agree, fragment targets resolve, and dates agree.
- GitHub Pages serves the validated commit before any indexing notification.
- Report verified actions separately from unavailable Search Console operations; do not claim ranking improvement.
